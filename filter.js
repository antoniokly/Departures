


function filterModes(modes, filters) {
    if (!filters) {
        return modes
    }

    const filtered = modes.filter( item =>  {
        return true
    });

    departureTimeMin, departureTimeMax, typeId

    return filtered;
}
//.result.transportation.modes;

module.exports = {
    filterModes
}